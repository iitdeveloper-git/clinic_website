import {notFound} from "next/navigation";
import Link from "next/link";
import {CTA,Footer,Header,PageHero} from "../../components";
import {clinic,doctors} from "../../data";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const doctor=doctors.find(d=>d.slug===slug);return doctor?{title:`${doctor.name} | ${clinic.name}`,description:"Consultant gynecologist profile. Verified credentials will be added before launch.",openGraph:{images:[]},twitter:{images:[]}}:{}}
export default async function DoctorProfile({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const doctor=doctors.find(d=>d.slug===slug);if(!doctor)notFound();return <><Header/><main><PageHero eyebrow="Doctor profile" title={doctor.name} intro="This page is ready for the doctor’s approved biography, photograph, qualifications and registration details."/><section className="content-section profile-layout"><aside className="profile-card" style={{background:doctor.color}}><div className="doctor-avatar">{doctor.initials}</div><h2>{doctor.name}</h2><p>{doctor.role}</p><div className="rating-pending">Rating will appear after verified reviews are supplied.</div><Link className="button" href="/appointment">Book Appointment</Link></aside><article className="profile-details"><p className="section-label">Professional information</p><h2>Verified profile details</h2><dl><div><dt>Qualification</dt><dd>{doctor.qualification}</dd></div><div><dt>Experience</dt><dd>{doctor.experience}</dd></div><div><dt>Registration</dt><dd>{doctor.registration}</dd></div><div><dt>Consultation timings</dt><dd>{doctor.availability}</dd></div></dl><div className="verify-note"><b>Why details are pending</b><p>We do not invent doctor credentials, years of experience, awards, specialties or ratings. These fields will be updated from the material you share.</p></div><h2>When to request a consultation</h2><p>Choose a relevant service or select “Not sure yet” on the appointment page. The doctor will determine appropriate advice or next steps after individual assessment.</p></article></section><CTA/></main><Footer/></>}
