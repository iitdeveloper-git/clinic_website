import {notFound} from "next/navigation";
import Image from "next/image";
import Link from "next/link";
import {CTA,Footer,Header,PageHero} from "../../components";
import {clinic,doctors} from "../../data";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const doctor=doctors.find(d=>d.slug===slug);return doctor?{title:`${doctor.name} | ${clinic.name}`,description:"Dr. Kanchan at Sampurna Clinic, Gyanpur, Bhadohi.",openGraph:{images:[]},twitter:{images:[]}}:{}}
export default async function DoctorProfile({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const doctor=doctors.find(d=>d.slug===slug);if(!doctor)notFound();return <><Header/><main><PageHero eyebrow="Doctor profile" title={doctor.name} intro="Women’s health, maternity and infertility consultation at Sampurna Clinic, Gyanpur, Bhadohi."/><section className="content-section profile-layout"><aside className="profile-card" style={{background:doctor.color}}><Image src="/sampurna-clinic-logo.png" alt="Sampurna Clinic logo" width={180} height={180} className="profile-logo"/><h2>{doctor.name}</h2><p>{doctor.role}</p><div className="rating-pending">Patient ratings will appear only after a verified source is supplied.</div><Link className="button" href="/appointment">Book Appointment</Link></aside><article className="profile-details"><p className="section-label">Professional information</p><h2>Doctor profile details</h2><dl><div><dt>Qualification</dt><dd>{doctor.qualification}</dd></div><div><dt>Area of care</dt><dd>{doctor.role}</dd></div><div><dt>Experience</dt><dd>{doctor.experience}</dd></div><div><dt>Registration</dt><dd>{doctor.registration}</dd></div><div><dt>Consultation timings</dt><dd>{doctor.availability}</dd></div><div><dt>Clinic</dt><dd>{clinic.address}</dd></div></dl><div className="verify-note"><b>Verification note</b><p>Qualification and speciality shown here come from the supplied clinic letterhead. Registration number, experience, timings, doctor photograph and ratings remain unpublished until verified.</p></div><h2>When to request a consultation</h2><p>Gynecology, pregnancy, maternity or infertility concern ke liye relevant service choose karein. Advice aur next steps individual doctor assessment par depend karte hain.</p></article></section><CTA/></main><Footer/></>}
