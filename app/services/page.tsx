import Link from "next/link";import {CTA,Footer,Header,PageHero} from "../components";import {services} from "../data";
export const metadata={title:"Women’s Health Services | Women’s Health Clinic",description:"Explore gynecology consultation pathways for women’s wellness, pregnancy, menstrual concerns, fertility, menopause and screening guidance."};
export default function Services(){return <><Header/><main><PageHero eyebrow="Women’s health services" title="Care designed around your questions and life stage." intro="Each service page explains the purpose of consultation without promising outcomes. Final clinical scope will be confirmed after the doctor shares verified information."/><section className="content-section service-grid">{services.map((s,i)=><article className="service-card" key={s.slug}><span>{s.icon}</span><small>0{i+1}</small><h2>{s.title}</h2><p>{s.short}</p><Link href={`/services/${s.slug}`}>Learn More →</Link></article>)}</section><CTA/></main><Footer/></>}
