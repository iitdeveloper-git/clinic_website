import Link from "next/link";import { CTA,Footer,Header,PageHero } from "../components";import { services } from "../data";
export const metadata={title:"Clinic Services | Aarogya Family Clinic",description:"Explore consultation, preventive and follow-up care at Aarogya Family Clinic."};
export default function Services(){return <><Header/><main><PageHero eyebrow="Clinic services" title="Care that starts with understanding." intro="Explore our consultation pathways. Exact clinical scope and available treatments must be confirmed with the clinic before launch."/><section className="content-section service-grid">{services.map(s=><article className="service-card" key={s.slug}><span>{s.icon}</span><h2>{s.title}</h2><p>{s.short}</p><Link href={`/services/${s.slug}`}>See what to expect →</Link></article>)}</section><CTA/></main><Footer/></>}
